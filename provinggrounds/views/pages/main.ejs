<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Menu - The Proving Grounds</title>
    <link rel="stylesheet" href="/provinggrounds/css/terminal.css">
</head>
<body>
    <div class="terminal">
        <div class="ascii-box">
<pre>
═══════════════════════════════════════════════════════
                 THE PROVING GROUNDS
                     Main Menu
═══════════════════════════════════════════════════════
</pre>
        </div>

        <% if (user.user_message) { %>
        <div class="message">
            [<%= user.name %>]: <%= user.user_message %>
        </div>
        <% } %>

        <div class="stats">
            <div class="stat">
                <span class="stat-label">Character:</span>
                <span class="stat-value"><%= user.name %></span>
            </div>
            <div class="stat">
                <span class="stat-label">Level:</span>
                <span class="stat-value"><%= user.level %> (<%= user.rank_title %>)</span>
            </div>
            <div class="stat">
                <span class="stat-label">Gold:</span>
                <span class="stat-value"><%= user.gold %></span>
            </div>
            <div class="stat">
                <span class="stat-label">HP:</span>
                <span class="stat-value"><%= user.hp %>/<%= user.max_hp %></span>
            </div>
            <div class="stat">
                <span class="stat-label">Time Left:</span>
                <span class="stat-value"><%= user.timeRemaining %> minutes</span>
            </div>
            <div class="stat">
                <span class="stat-label">Calls Today:</span>
                <span class="stat-value"><%= user.calls_today %>/<%= user.calls_per_day %></span>
            </div>
        </div>

        <% if (unreadMail > 0) { %>
        <div class="message message-warning">
            You have <%= unreadMail %> unread message(s)!
        </div>
        <% } %>

        <h2>Commands</h2>

        <div class="menu">
            <div class="menu-item">
                <span class="menu-key">A)</span>
                <a href="/stores" data-key="a">Enter the Bazaar/Go Shopping</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">B)</span>
                <a href="/boards" data-key="b">Board Access</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">D)</span>
                <a href="/combat" data-key="d">Dungeon/Fight Users and Monsters</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">G)</span>
                <a href="/games" data-key="g">Gambling</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">%)</span>
                <a href="/games/joust" data-key="%">The Proving Downs/Joust</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">K)</span>
                <a href="/main/treasury" data-key="k">Castle Treasury</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">L)</span>
                <a href="/main/ladder" data-key="l">The Ladder/User Rankings</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">M)</span>
                <a href="/main/members" data-key="m">Member Listing</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">N)</span>
                <a href="/main/news" data-key="n">News/Updates</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">V)</span>
                <a href="/main/vote" data-key="v">Voting Booth</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">Y)</span>
                <a href="/main/stats" data-key="y">Your Stats/View Character</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">@)</span>
                <a href="/main/password" data-key="@">Change Password</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">^)</span>
                <a href="/main/help" data-key="^">View Help File</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">O)</span>
                <a href="/logout" data-key="o">Logoff</a>
            </div>
        </div>

        <div class="prompt">
            <form action="/main/message" method="POST" style="flex: 1; display: flex;">
                <input type="text" name="message" placeholder="User message (40 char max)" maxlength="40" style="flex: 1;">
                <button type="submit" style="margin-left: 10px;">Set</button>
            </form>
        </div>

        <div class="status-bar">
            <span>[<%= user.name %>]</span>
            <span>Level: <%= user.level %></span>
            <span>Gold: <%= user.gold %></span>
            <span>HP: <%= user.hp %>/<%= user.max_hp %></span>
            <span>Time: <%= user.timeRemaining %>m</span>
        </div>
    </div>

    <script>
        // Keyboard navigation - don't trigger when typing in input fields
        document.addEventListener('keydown', (e) => {
            if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;

            // Try the raw key first (for special chars like @, %, ^), then lowercase
            let link = document.querySelector(`a[data-key="${e.key}"]`);
            if (!link) {
                link = document.querySelector(`a[data-key="${e.key.toLowerCase()}"]`);
            }
            if (link) {
                e.preventDefault();
                window.location.href = link.href;
            }
        });
    </script>
    <script src="/provinggrounds/js/terminal.js"></script>
</body>
</html>
