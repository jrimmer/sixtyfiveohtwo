<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Combat Menu - The Proving Grounds</title>
    <link rel="stylesheet" href="/provinggrounds/css/terminal.css">
</head>
<body>
    <div class="terminal">
        <div class="ascii-box">
<pre>
===============================================
          DUNGEON ENTRANCE
      "Abandon hope all ye who enter"
===============================================
</pre>
        </div>

        <div class="stats" style="margin: 20px 0;">
            <div class="stat">
                <span class="stat-label">HP:</span>
                <span class="stat-value"><%= user.hp %>/<%= user.max_hp %></span>
            </div>
            <div class="stat">
                <span class="stat-label">Power:</span>
                <span class="stat-value"><%= user.power %>/<%= user.max_power %></span>
            </div>
            <div class="stat">
                <span class="stat-label">Weapon:</span>
                <span class="stat-value"><%= user.weapon_name %></span>
            </div>
            <div class="stat">
                <span class="stat-label">Armor:</span>
                <span class="stat-value"><%= user.armor_name %></span>
            </div>
        </div>

        <h2>Combat Options</h2>

        <div class="menu">
            <div class="menu-item">
                <span class="menu-key">P)</span>
                <a href="/provinggrounds/combat/proving-grounds">The Proving Grounds (Fight Users)</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">D)</span>
                <a href="/provinggrounds/combat/dungeon">Enter the Dungeon (Fight Monsters)</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">C)</span>
                <a href="/provinggrounds/combat/corridor">Corridor of Death</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">A)</span>
                <a href="/provinggrounds/combat/castle">Attack the Castle</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">&lt;</span>
                <a href="/provinggrounds/main">Return to Main Menu</a>
            </div>
        </div>

        <% if (user.hp < user.max_hp / 2) { %>
        <div class="message message-warning">
            Warning: Your HP is low! Consider visiting the healer.
        </div>
        <% } %>

        <div class="status-bar">
            <span>[<%= user.name %>]</span>
            <span>Level: <%= user.level %></span>
            <span>Gold: <%= user.gold %></span>
            <span>HP: <%= user.hp %>/<%= user.max_hp %></span>
        </div>
    </div>
    <script src="/provinggrounds/js/terminal.js"></script>
</body>
</html>
