<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roulette - The Proving Grounds</title>
    <link rel="stylesheet" href="/css/terminal.css">
</head>
<body>
    <div class="terminal">
        <div class="ascii-box">
<pre>
===============================================
             ROULETTE
      "Place your bets!"
===============================================
</pre>
        </div>

        <% if (typeof message !== 'undefined' && message) { %>
        <div class="message message-success">
            <%= message %>
        </div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
        <div class="message message-error">
            <%= error %>
        </div>
        <% } %>

        <% if (typeof result !== 'undefined') { %>
        <div class="ascii-box" style="text-align: center; font-size: 1.5em;">
            The wheel stops on: <span style="color: <%= result.color === 'red' ? 'var(--error-color)' : (result.color === 'green' ? 'var(--text-color)' : 'var(--dim-color)') %>;"><%= result.number %> (<%= result.color %>)</span>
        </div>
        <% } %>

        <div class="stats" style="margin: 20px 0;">
            <div class="stat">
                <span class="stat-label">Gold:</span>
                <span class="stat-value"><%= user.gold %></span>
            </div>
        </div>

        <h3>Place Your Bet</h3>

        <form action="/games/roulette/play" method="POST">
            <div class="form-group">
                <label for="bet">Bet Amount:</label>
                <input type="number" name="bet" id="bet" min="1" max="<%= user.gold %>" value="10">
            </div>

            <div class="form-group">
                <label for="type">Bet Type:</label>
                <select name="type" id="type">
                    <option value="red">Red (2x)</option>
                    <option value="black">Black (2x)</option>
                    <option value="odd">Odd (2x)</option>
                    <option value="even">Even (2x)</option>
                    <option value="low">Low 1-18 (2x)</option>
                    <option value="high">High 19-36 (2x)</option>
                    <option value="number">Specific Number (36x)</option>
                </select>
            </div>

            <div class="form-group" id="number-select" style="display: none;">
                <label for="number">Number (0-36):</label>
                <input type="number" name="number" id="number" min="0" max="36" value="0">
            </div>

            <div class="form-group">
                <button type="submit">Spin the Wheel!</button>
            </div>
        </form>

        <div class="menu" style="margin-top: 20px;">
            <div class="menu-item">
                <span class="menu-key">&lt;</span>
                <a href="/games">Return to Gambling</a>
            </div>
        </div>

        <div class="status-bar">
            <span>[<%= user.name %>]</span>
            <span>Level: <%= user.level %></span>
            <span>Gold: <%= user.gold %></span>
            <span>HP: <%= user.hp %>/<%= user.max_hp %></span>
        </div>
    </div>
    <script>
        document.getElementById('type').addEventListener('change', function() {
            document.getElementById('number-select').style.display =
                this.value === 'number' ? 'block' : 'none';
        });
    </script>
    <script src="/js/terminal.js"></script>
</body>
</html>
