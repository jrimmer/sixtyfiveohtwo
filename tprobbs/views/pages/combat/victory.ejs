<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Victory! - Lost Gonzo BBS</title>
    <link rel="stylesheet" href="/provinggrounds/css/terminal.css">
</head>
<body>
    <div class="terminal">
        <div class="ascii-box">
<pre>
===============================================================
                       VICTORY!
===============================================================
</pre>
        </div>

        <div class="message message-success">
            <p>You defeated the monster!</p>
            <p>Gained <%= xp.toLocaleString() %> XP</p>
            <p>Found <%= gold.toLocaleString() %> gold</p>
        </div>

        <% if (levelUp.leveledUp) { %>
        <div class="message message-warning">
            <h3>LEVEL UP!</h3>
            <p>You are now level <%= levelUp.newLevel %>!</p>
            <p>HP increased by <%= levelUp.hpGain %></p>
            <% if (levelUp.spGain > 0) { %>
            <p>SP increased by <%= levelUp.spGain %></p>
            <% } %>
        </div>
        <% } %>

        <div class="menu">
            <div class="menu-item">
                <span class="menu-key">C)</span>
                <a href="/tprobbs/combat/dungeon">Continue Exploring</a>
            </div>
            <div class="menu-item">
                <span class="menu-key">L)</span>
                <a href="/tprobbs/combat/dungeon/leave">Leave Dungeon</a>
            </div>
        </div>

        <div class="status-bar">
            <span>[<%= user.username %>]</span>
            <span>Level: <%= user.level %></span>
            <span>Gold: <%= Math.floor(user.gold).toLocaleString() %></span>
            <span>HP: <%= user.hit_points %>/<%= user.max_hp %></span>
            <span>Time: <%= user.timeRemaining %>m</span>
        </div>
    </div>
    <script src="/provinggrounds/js/terminal.js"></script>
</body>
</html>
