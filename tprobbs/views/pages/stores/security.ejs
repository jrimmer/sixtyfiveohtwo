<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Security Systems - Lost Gonzo BBS</title>
    <link rel="stylesheet" href="/provinggrounds/css/terminal.css">
</head>
<body>
    <div class="terminal">
        <div class="ascii-box">
<pre>
===============================================================
                  SECURITY SYSTEMS
                 "Protect Your Gold"
===============================================================
</pre>
        </div>

        <div class="stats">
            <div class="stat">
                <span class="stat-label">Gold:</span>
                <span class="stat-value"><%= Math.floor(user.gold).toLocaleString() %></span>
            </div>
            <div class="stat">
                <span class="stat-label">Current System:</span>
                <span class="stat-value"><%= currentSecurity ? currentSecurity.name : 'None' %></span>
            </div>
        </div>

        <% if (message) { %>
        <div class="message <%= messageType === 'error' ? 'message-error' : 'message-success' %>">
            <p><%= message %></p>
        </div>
        <% } %>

        <p>Security systems add additional protection against thieves!</p>
        <p>Combines with your home's protection level.</p>

        <table>
            <thead>
                <tr>
                    <th>System</th>
                    <th>Level</th>
                    <th>Protection</th>
                    <th>Price</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% systems.forEach(s => { %>
                <tr>
                    <td><%= s.name %></td>
                    <td><%= s.level %></td>
                    <td>+<%= s.protection %>%</td>
                    <td><%= s.price.toLocaleString() %></td>
                    <td>
                        <% if (user.security >= s.id) { %>
                        <span class="owned">Owned</span>
                        <% } else if (user.gold >= s.price) { %>
                        <form action="/tprobbs/stores/security/buy/<%= s.level %>" method="POST" style="display:inline">
                            <button type="submit" class="link-button">Buy</button>
                        </form>
                        <% } else { %>
                        <span class="insufficient">Insufficient</span>
                        <% } %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>

        <div class="menu">
            <div class="menu-item">
                <span class="menu-key">B)</span>
                <a href="/tprobbs/stores">Back to Stores</a>
            </div>
        </div>

        <div class="status-bar">
            <span>[<%= user.username %>]</span>
            <span>Level: <%= user.level %></span>
            <span>Gold: <%= Math.floor(user.gold).toLocaleString() %></span>
            <span>HP: <%= user.hit_points %>/<%= user.max_hp %></span>
            <span>Time: <%= user.timeRemaining %>m</span>
        </div>
    </div>
    <script src="/provinggrounds/js/terminal.js"></script>
</body>
</html>
